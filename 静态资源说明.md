# 静态资源路径说明

## 📁 目录结构

在 Vite 项目中，静态资源有两种存放方式：

### 1. `public` 目录（推荐用于大文件）

**位置**：项目根目录下的 `public` 文件夹

**特点**：
- 文件会直接复制到构建输出目录的根目录
- 访问路径以 `/` 开头
- 适合存放：视频、大图片、字体文件等

**示例**：
```
public/
  ├── model-video.mp4          → 访问路径：/model-video.mp4
  ├── placeholder-video.jpg    → 访问路径：/placeholder-video.jpg
  └── images/
      └── logo.png             → 访问路径：/images/logo.png
```

### 2. `src/assets` 目录（推荐用于小文件）

**位置**：`src/assets` 文件夹

**特点**：
- 文件会被 Vite 处理（压缩、优化等）
- 需要通过 `import` 导入
- 适合存放：小图片、图标等

**示例**：
```vue
<script>
import logo from '@/assets/logo.png'
</script>
<template>
  <img :src="logo" alt="logo">
</template>
```

## 🎥 视频文件使用

### 当前项目中的视频

在 `src/views/Home.vue` 中，视频路径配置为：

```javascript
data() {
  return {
    videoSrc: '/model-video.mp4',        // 视频文件路径
    videoPoster: '/placeholder-video.jpg' // 视频封面图路径
  }
}
```

### 添加视频文件步骤

1. **将视频文件放入 `public` 目录**：
   ```
   public/
     └── model-video.mp4
   ```

2. **将封面图放入 `public` 目录**：
   ```
   public/
     └── placeholder-video.jpg
   ```

3. **访问路径**：
   - 视频：`/model-video.mp4`
   - 封面：`/placeholder-video.jpg`

## 🖼️ 图片文件使用

### 当前项目中的图片

其他页面（红色资源、绿色资源、地质资源）使用的图片是占位符，使用的是在线占位符服务：

```html
<img src="https://via.placeholder.com/400x250/667eea/ffffff?text=图片标题">
```

### 替换为本地图片

**方法一：使用 public 目录（推荐）**

1. 在 `public` 目录下创建 `images` 文件夹：
   ```
   public/
     └── images/
         ├── jinchaji-1.jpg
         ├── luotuowan-1.jpg
         └── ...
   ```

2. 在 Vue 组件中使用：
   ```html
   <img src="/images/jinchaji-1.jpg" alt="晋察冀纪念馆">
   ```

**方法二：使用 assets 目录**

1. 在 `src/assets` 目录下创建 `images` 文件夹：
   ```
   src/
     └── assets/
         └── images/
             ├── jinchaji-1.jpg
             └── ...
   ```

2. 在 Vue 组件中导入：
   ```vue
   <script>
   import jinchajiImg from '@/assets/images/jinchaji-1.jpg'
   </script>
   <template>
     <img :src="jinchajiImg" alt="晋察冀纪念馆">
   </template>
   ```

## 📝 总结

| 资源类型 | 推荐位置 | 访问方式 | 示例 |
|---------|---------|---------|------|
| 视频文件 | `public/` | `/filename.mp4` | `/model-video.mp4` |
| 大图片 | `public/images/` | `/images/filename.jpg` | `/images/photo.jpg` |
| 小图片/图标 | `src/assets/` | `import` 导入 | `import img from '@/assets/logo.png'` |
| 字体文件 | `public/fonts/` | `/fonts/font.ttf` | `/fonts/custom.ttf` |

## ⚠️ 注意事项

1. **public 目录下的文件路径**：
   - 必须以 `/` 开头
   - 路径相对于项目根目录
   - 不会经过 Vite 处理

2. **assets 目录下的文件**：
   - 必须通过 `import` 导入
   - 会经过 Vite 处理（压缩、优化）
   - 支持动态导入

3. **开发环境 vs 生产环境**：
   - 开发环境：`public` 目录的文件可以直接访问
   - 生产环境：`public` 目录的文件会被复制到 `dist` 目录





